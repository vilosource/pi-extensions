; Pin the registry so `npm install` works for outside contributors whose
; global ~/.npmrc points at a private registry. See the agent-spend /
; pi-extensions convention: every published package carries this pin.
registry=https://registry.npmjs.org/
